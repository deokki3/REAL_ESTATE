# CLAUDE.md

부동산 공고 통합 + 자금 설계 서비스. 이 파일은 매 세션에서 자동 로드된다.
새로운 결정이 나오면 **반드시 이 파일에 반영한다.** 여기 없는 결정은 다음 세션에서 사라진다.

---

## 1. 이 서비스가 무엇인가

> 흩어진 공공주택 공고를 한곳에서 보고, 내 자본과 자격으로 실제로 가능한지
> 필요 자금과 월 상환액까지 계산해주는 서비스

핵심 가치는 **공고 통합이 아니라 자금·자격 판정**이다.
공고 데이터는 계산기의 입력값이다. 이 순서를 뒤집지 말 것.

### 안 만드는 것
- 청약 신청 대행 (실제 신청은 해당 기관에서)
- 법적 효력이 있는 자격 판정 → 어디까지나 참고용 사전 검토
- 시세 예측

---

## 2. 협업 방식

- 사용자는 5년차 퍼블리싱/개발자. Java / Spring MVC / MyBatis 경험.
  기본 개념 설명은 불필요. **Java에서 하던 것과 무엇이 다른지**를 짚어줄 것.
- 코드는 Claude가 작성한다. 사용자가 모르는 부분을 질문하는 방식.
- 큰 기능을 한 번에 만들지 않는다. 작은 단위로 나눠 진행.
- 요구사항이 불명확하거나 충돌하면 임의 결정하지 말고 먼저 질문한다.
- 기존 코드를 확인하지 않고 파일 구조를 추측하지 않는다.
- **사용자에게 보이는 모든 텍스트는 한글.** 답변뿐 아니라 도구 실행 설명과 선택지 라벨까지.

---

## 3. 기술 스택 및 구조

```
Real_estate/
├─ client/    React + TypeScript + Vite + TanStack Query + Tailwind v4
└─ server/    Node + Express + TypeScript + Mongoose
```

- client / server 는 **독립 npm 프로젝트**. 모노레포 아님.
  → 타입이 양쪽에 존재한다. 서버 응답 형태를 바꾸면 클라이언트도 수동 동기화 필요.
- Tailwind는 **v4**. `@tailwindcss/vite` 플러그인 + CSS의 `@import "tailwindcss";`
  `tailwind.config.js` / `postcss.config.js` / `npx tailwindcss init` 은 v3 방식이며 사용하지 않는다.
- 개발 중 CORS 없음 → Vite 프록시가 `/api/*` 를 4000 포트로 전달.
  클라이언트 코드에 서버 주소를 하드코딩하지 않는다.
- 의존성 버전은 package.json에 고정하지 않았다. README의 install 명령 참조.
- **서버는 ESM(`"type": "module"`, tsconfig `NodeNext`)** 이라 상대 경로 import에 `.js` 확장자를 붙인다.
  실제 파일이 `.ts` 여도 `./health.service.js` 라고 쓴다. 클라이언트(Vite)는 이 규칙이 없다.

### 계층
```
Frontend → API → Controller → Service → Repository/Model → MongoDB
```
비즈니스 로직은 Service 중심. 응답은 항상 `{ success, data, meta? }` 봉투.
실패는 `{ success: false, error: { code, message } }`.

---

## 4. 확정된 설계 결정

각 항목은 근거가 있다. 근거를 이해하지 않은 채 바꾸지 말 것.

### D1. 외부 API를 프론트에서 직접 호출하지 않는다
인증키 노출 + CORS + 비표준 응답 형태. 서버는 우회 계층이 아니라 **정규화 계층**이다.

### D2. 수집(ingest)과 조회(query)를 분리한다
```
server/src/ingest/    외부 API만 안다. HTTP 요청·사용자·페이지네이션 개념 없음
server/src/modules/   우리 DB만 안다. 외부 API를 모른다
```
수집 진입점은 순수 함수 하나. 호출자가 온디맨드든 배치든 스케줄러든 무관.
**이 경계를 깨면 수집 방식 변경 시 조회 코드를 전부 뜯게 된다.**

### D3. Service 계층은 req/res에 의존하지 않는다
```ts
searchAnnouncements(criteria: SearchCriteria): Promise<SearchResult>   // OK
searchAnnouncements(req: Request, res: Response)                       // 금지
```
이유: 나중에 AI Agent가 같은 함수를 도구로 호출해야 한다.
AI 기능은 나중에 만들더라도 이 제약은 **지금부터** 적용한다.

### D4. 소스 어댑터 패턴
소스마다 수집 방식이 다르다 (오픈API / 파일데이터 / 없음).
각 어댑터는 자기 소스 응답을 공통 `Announcement`로 변환하는 책임만 갖는다.
조회 계층과 화면은 데이터 출처를 모른다. `Announcement.source` 는 필수 필드.

### D5. 저장할 값과 계산할 값을 구분한다
진행상태(모집중/마감)를 DB에 저장하지 않는다. 접수 시작·종료일만 저장하고
조회 시점에 계산한다. 저장하면 접수가 끝나도 영원히 "모집중"이 된다.

**실제 구현 시 제약 발견 (2026-08-27):** LH 목록 API는 접수시작일을 안 준다 — 게시일(`PAN_NT_ST_DT`)과
마감일(`CLSG_DT`)만 있고, 청약홈처럼 `RCEPT_BGNDE`/`RCEPT_ENDDE` 같은 필드가 없다. 그래서 원래
`PAN_SS`가 보여주던 3단계(공고중/접수중/접수마감)를 우리가 그대로 재현할 수 없고, 마감일 기준
2단계(`open`/`closed`)만 계산 가능하다. `AnnouncementStatus`가 두 값뿐인 이유가 이것이다 — 데이터가
없어서 못 만드는 것이지 설계를 단순화한 게 아니다. (상세 API가 언젠가 뚫리면 접수기간 필드가 있는지
확인할 것 — 7절.)

### D6. 도메인 축을 분리한다
| 축 | 성격 |
|---|---|
| `Announcement` | 앞으로 열리는 기회 (공고) |
| `Transaction` | 이미 일어난 사실 (실거래) |
| `Listing` | 현재 시장 공급 (v1 범위 밖) |

성격이 달라 한 컬렉션으로 합치지 않는다. 공통 앵커는 `Region`(법정동코드)과 단지 식별자.

### D7. 분양과 임대의 자금 구조를 하나의 계산기로 만들지 않는다
| | 분양·매매 | 임대 |
|---|---|---|
| 지불 | 계약금 → 중도금 → 잔금 | 보증금 + 월세 |
| 대출 | 중도금대출 → 주택담보대출 | 전세자금대출 |
| 선정 | 청약가점 또는 추첨 | 계층별 자격 + 공고별 배점 |

`SalePlan` / `LeasePlan` 으로 나누고 공통 인터페이스만 맞춘다.

계약금(10~20%)에 대출이 거의 안 되는 자기자본이다.
`분양가 × LTV` 로 계산하면 틀린다. 시뮬레이터가 답할 질문은
"빌릴 수 있냐"가 아니라 **"각 시점에 현금이 마르지 않냐"** 이다.

### D8. 자격·대출 기준은 코드가 아니라 데이터다
소득 기준, 가점 배점, LTV/DSR 한도, 특별공급 비율은 법령·지침 개정으로 바뀐다.
`if (income <= 1.3 * median)` 을 코드에 박으면 개정 때마다 전수 수정이고,
과거 공고를 그 시점 기준으로 재현할 수 없다.

**적용 시작일을 가진 룰 데이터**로 저장하고 판정 엔진이 룰을 읽어 실행한다.

### D9. 대출 결과를 단정하지 않는다
실제 한도·금리는 개인 신용과 은행 심사에 좌우된다. 우리는 알 수 없다.
```
금지: "디딤돌대출 3.2억 승인 가능"
허용: "입력 조건상 디딤돌대출 신청 요건에 해당합니다.
       금리 3.5% 가정 시 월 상환액 약 168만원.
       실제 한도·금리는 은행 심사에 따라 달라집니다."
```
금리 등 가정값은 사용자가 조정할 수 있게 한다.

### D10. 데이터 제공 범위를 명시한다
SH·HUG 공고는 현재 확보 불가. 그냥 안 보여주면 사용자는 "공고가 없나 보다"로 오해한다.
"현재 LH·청약홈 공고를 제공합니다. 그 외는 각 기관 사이트에서 확인하세요" 안내와 링크 필수.

---

## 5. 도메인 용어 → 코드 이름 규정

부동산은 용어를 확정하지 않으면 코드가 반드시 섞인다.

### 면적: `area` 단독 사용 금지
| 용어 | 코드 이름 |
|---|---|
| 전용면적 (실거래가 API 기준) | `exclusiveAreaM2` |
| 공급면적 (분양정보 기준) | `supplyAreaM2` |
| 계약면적 | `contractAreaM2` |
| 주택형 (`84A` = 전용 84㎡ A타입) | `houseType` |

같은 집이 전용 84㎡ / 공급 112㎡ 로 표기된다.
**실거래가 API는 전용, 청약 분양정보는 공급을 준다.** 섞이면 다른 집이 된다.
비교·필터는 전부 `exclusiveAreaM2` 기준으로 통일.

### 가격: `price` 단독 사용 금지
| 용어 | 코드 이름 |
|---|---|
| 분양가 | `supplyPriceWon` |
| 실거래가 | `dealAmountWon` |
| 보증금 | `depositWon` |
| 월세 | `monthlyRentWon` |

**내부 저장 단위는 원 단위 정수로 통일한다 (2026-08-25 정정 — 이전엔 만원 단위였음).**
LH 공급정보 API의 임대보증금(`LS_GMY`)·월임대료(`MM_RFE`)가 만원 단위로 딱 안 떨어지는 원 단위 값으로 온다
(예: `68,801,000원` = 6880.1만원). 만원 단위로 반올림해서 저장하면 그 차액이 영구히 사라진다.
사용자가 원 단위 정확성을 요구해서 저장은 원 단위로, **표시 단계에서만** `Math.floor(원값 / 10000)` 등으로 "12억 3,000만원" 포맷.
원 단위 혼용 금지는 그대로 유지 — 어떤 필드도 만원 단위로 저장하지 않는다.

### 기타 주의
- **모집공고일이 자격 산정 기준일**이다. 무주택기간·통장 가입기간은 오늘이 아니라
  모집공고일 기준으로 계산한다. 이걸 틀리면 자격 판정이 통째로 틀린다.
- **행복주택에는 청약가점(84점 만점)이 없다.** 그건 민영주택 일반공급 방식이다.
  행복주택은 공급 대상 계층 → 자격 충족 여부 → 공고별 순위·배점/추첨.
  "자격 충족 여부"와 "경쟁 시 순위"를 하나의 점수로 묶지 말 것.
- 국민주택과 민영주택은 자격 요건과 선정 방식이 다르다. 한 판정 로직으로 묶지 않는다.

---

## 6. LH 분양임대공고문 조회 API — 검증된 사실

활용가이드 문서(20260708)에서 확인한 내용. **추측 아님.**

### 엔드포인트
```
http://apis.data.go.kr/B552555/lhLeaseNoticeInfo1/lhLeaseNoticeInfo1
```
- **HTTP다. HTTPS 아님.** 브라우저 직접 호출 시 mixed content로 차단. 서버 경유 필수.
- 응답은 JSON.
- 초당 최대 30 tps, 개발계정 일일 트래픽 10,000.

### 요청 파라미터
| 항목 | 필수 | 설명 |
|---|---|---|
| `serviceKey` | O | URL Encode |
| `PG_SZ` | O | 페이지 크기 |
| `PAGE` | O | 페이지 번호 |
| `PAN_ST_DT` | O | 게시일 시작 (YYYYMMDD, 초기값 현재일-2개월) |
| `PAN_ED_DT` | O | 게시일 종료 (YYYYMMDD, 초기값 현재일) |
| `PAN_NM` | | 공고명 |
| `UPP_AIS_TP_CD` | | 공고유형코드 |
| `CNP_CD` | | 지역코드 |
| `PAN_SS` | | 공고상태 |
| `CLSG_ST_DT` / `CLSG_ED_DT` | | 마감일 기간 |

**게시일 기간이 필수다.** 수집기는 기간 단위로 동작한다.

### 코드값
```
UPP_AIS_TP_CD  01 토지 / 05 분양주택 / 06 임대주택 / 13 주거복지 / 22 상가 / 39 신혼희망타운
               → 우리는 05, 06, 13, 39만 사용. 토지(01), 상가(22) 제외.

CNP_CD         11 서울 / 28 인천 / 41 경기  (수도권. 그 외 코드는 가이드 참조)

PAN_SS         공고중 / 접수중 / 접수마감 / 안내신청 / 선정공고중
```

`AIS_TP_CD`(매물유형코드) 전체 목록 — 상세/공급정보 활용가이드(20260616/20260202)에서 확보. 목록 API 문서에는 없던 표다.
```
01 토지 / 02 주택용지 / 03 상가용지 / 04 산업시설용지 / 05 분양주택 / 06 임대주택
07 국민임대 / 08 공공임대 / 09 영구임대 / 10 행복주택 / 11 장기전세
13 매입임대/전세임대 / 17 전세임대 / 23 분양·(구)임대상가(입찰) / 24 임대상가(추첨)
26 매입임대 / 27 신축다세대매입임대 / 36·37 집주인임대 / 38 임대상가(공모·심사)
39 공공분양(신혼희망) / 40 가정어린이집 / 42 행복주택(신혼희망) / 43 임대상가(입찰)
45 사전청약 / 48 통합공공임대 / 51 통합공공임대(신혼희망) / 52 6년 공공임대주택
54 이익공유형 공공분양주택 / 99 기타
```

### 응답 구조 — 비표준. 어댑터에서 반드시 흡수할 것
```json
[
  { "dsSch": [ { 검색조건 요약 } ] },
  { "resHeader": [ { "SS_CODE": "Y", "RS_DTTM": "..." } ],
    "dsList": [ { 공고... } ] }
]
```
최상위가 객체가 아니라 **배열**이다. 실제 데이터는 `response[1].dsList`.
`data.body.items` 같은 표준 구조가 아니다. 이 형태를 Service 이하로 흘려보내지 말 것.

**에러 응답은 완전히 다른 봉투로 온다 (2026-08-25 실제 확인, 3종 API 공통).**
정상 응답은 위처럼 배열이지만, `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`(30)·`NO_OPENAPI_SERVICE_ERROR`(12)·
`SERVICETIMEOUT_ERROR`(05) 등 에러는 전부 `{"OpenAPI_ServiceResponse": {"cmmMsgHeader": {"errMsg", "returnAuthMsg", "returnReasonCode"}}}`
형태의 **객체**로 온다. 파서는 최상위가 배열인지 먼저 보고, 배열이 아니면 `cmmMsgHeader.returnReasonCode`로 UpstreamError를 던지는 식으로
분기해야 한다. 정상 응답의 `resHeader.SS_CODE`와 에러 응답의 `cmmMsgHeader.returnReasonCode`는 서로 다른 자리에 다른 이름으로 있다는 점 주의.

### 주요 응답 필드
| 필드 | 뜻 |
|---|---|
| `PAN_ID` | 공고 아이디 → **상세/공급정보 API의 요청 키** |
| `CCR_CNNT_SYS_DS_CD` | 고객센터연계시스템구분코드 → 상세 조회 시 필요 |
| `UPP_AIS_TP_CD` / `AIS_TP_CD` | 상위/매물 유형코드 → 상세 조회 시 필요 |
| `PAN_NM` | 공고명 |
| `UPP_AIS_TP_NM` / `AIS_TP_CD_NM` | 공고유형명 / 세부유형명 (예: 임대주택 / 행복주택) |
| `CNP_CD_NM` | 지역명 |
| `PAN_NT_ST_DT` | 공고게시일 → 포맷 `2019.07.23` |
| `CLSG_DT` | 공고마감일 → 포맷 `2019.08.22` |
| `PAN_DT` | 모집공고일 → 포맷 `20200508` (토지·상가는 미제공) |
| `PAN_SS` | 공고상태 |
| `ALL_CNT` | 전체 조회 건수 → 페이지네이션 종료 조건 |
| `DTL_URL` / `DTL_URL_MOB` | 공고 상세 URL |

**연결 키는 실제로 5개 조합이다 (2026-08-25 정정):** `PAN_ID` + `CCR_CNNT_SYS_DS_CD` + `UPP_AIS_TP_CD` + `AIS_TP_CD` + `SPL_INF_TP_CD`.
당초 4개로 기록했었는데, 실제 목록 API 응답에 `SPL_INF_TP_CD`(공급정보구분코드)가 매 항목마다 들어있고
상세·공급정보 API 양쪽 다 이 값을 필수 파라미터로 요구한다는 걸 실제 호출로 확인했다. 목록 API 활용가이드 자체에는
이 필드 설명이 없었지만(6절 "미확인 사항"에 있던 항목), 상세/공급정보 API 활용가이드를 받아보고 나서
"분양임대공고문조회 API 의 응답 메시지 중 공급정보구분코드"라는 설명으로 정체가 밝혀졌다.
하나라도 저장을 빠뜨리면 상세·공급정보 조회가 불가능하다.

**날짜 포맷이 한 응답에 두 종류로 섞여 나온다.** (`2019.07.23` 와 `20200508`)
정규화 계층에서 전부 Date로 변환할 것. 문자열로 저장하면 기간 검색·정렬이 깨진다.

### 관련 API (3종 세트) — 2026-08-25 전부 활용신청 승인, 목록·공급정보 실호출 검증 완료

| 데이터셋 | 서비스 | 역할 | 상태 |
|---|---|---|---|
| 15058530 | `lhLeaseNoticeInfo1` | 공고 목록 | ✅ 실호출 성공, 6절 기존 내용과 응답 구조 일치 확인 |
| 15057999 | `lhLeaseNoticeDtlInfo1` | 공고 상세 | ⚠️ URL·파라미터는 활용가이드로 확보했으나 실호출 2회 모두 `SERVICETIMEOUT_ERROR(05)` — 우리 키·URL 문제가 아니라 LH 백엔드 쪽 문제로 추정 (아래 상세 참고) |
| 15056765 | `lhLeaseNoticeSplInfo1` | 주택형/필지/상가호 목록 (면적·가격) | ✅ 실호출 성공 |

목록 API에는 면적도 임대료도 없다. 정렬·필터를 하려면 공급정보 API가 필요하다.

**목록 API와 상세·공급정보 API는 요청 URL 패턴이 다르다.** 목록은 `.../lhLeaseNoticeInfo1/lhLeaseNoticeInfo1`
(경로가 API명과 똑같이 한 번 더 반복)인데, 상세·공급정보는 `.../lhLeaseNoticeDtlInfo1/getLeaseNoticeDtlInfo1`,
`.../lhLeaseNoticeSplInfo1/getLeaseNoticeSplInfo1` 처럼 `get` 접두사가 붙은 별도 오퍼레이션명이 붙는다.
이 패턴을 목록 API와 같다고 추측해서 호출했다가(경로 반복형 시도) `12번(NO_OPENAPI_SERVICE_ERROR)`로 실패한 적 있다 —
**API마다 URL 패턴을 절대 유추하지 말고 각자의 활용가이드 문서를 확인할 것.**

#### 15056765 공급정보 API — 검증됨
- 요청: `serviceKey` + `SPL_INF_TP_CD`(필수) + `CCR_CNNT_SYS_DS_CD`(필수) + `PAN_ID`(필수) + `UPP_AIS_TP_CD`(필수) + `AIS_TP_CD`(옵션)
- 응답은 **`SPL_INF_TP_CD` 값에 따라 리스트 이름과 필드가 완전히 다르다.** 예: `SPL_INF_TP_CD=060`(공공임대 5/10년·분납임대) 그룹은
  `dsList01`(공공분양: `SIL_AMT` 평균분양가격 포함) / `dsList02`(공공임대: `LS_GMY` 임대보증금·`MM_RFE` 월임대료) / `dsList03`(분납임대: `ELY_DSU_AMT` 초기분납금)
  세 리스트를 동시에 내려주고, 해당 없는 리스트는 빈 배열로 온다.
- 실제 호출(경기 김포한강 Ac-05, `SPL_INF_TP_CD=060`)에서 `dsList02`에 주택형별 전용면적·공급면적·세대수·임대보증금·월임대료가 정상적으로 왔다.
  이게 상세 화면 "주택형별 공급 정보" 표의 실제 데이터 소스다.
- **활용가이드에 없는 `dsList04`가 실제 응답에 나왔다** — 필드 구성이 `dsList02`(공공임대)와 동일. 용도 불명. 5년/10년 공공임대 구분일 가능성이 있으나
  문서에 근거가 없어 단정하지 않는다. 7절 미확인 사항 참고.
- **`SPL_INF_TP_CD=061/062/063`(50년공공임대/국민임대·장기전세·신축다세대·영구임대/행복주택)은 응답 리스트 이름이 또 `dsList01`이다 —
  그런데 050/060 그룹의 `dsList01`(공공분양)과는 필드 구성이 완전히 다르다.** 같은 리스트 이름이 브랜치마다 다른 뜻으로 재사용되므로,
  호출 시점에 어떤 `SPL_INF_TP_CD`로 불렀는지가 응답을 해석하는 유일한 근거다. 필드: `SBD_LGO_NM`(단지명)·`HTY_NNA`(주택형)·
  `DDO_AR`(전용면적)·`HSH_CNT`(세대수)·`NOW_HSH_CNT`(금회공급세대수)·`LS_GMY`(임대보증금)·`RFE`(월임대료)·`SPL_AR`(공급면적).
  실제 호출(2026-08-27, 서울번동3 행복주택 `SPL_INF_TP_CD=063`)로 검증 완료.
- **국민임대·행복주택 계열은 임대보증금·월세를 숫자로 안 준다 — 실제로 확인됨 (2026-08-27).**
  `LS_GMY`·`RFE` 값이 숫자가 아니라 **"공고문 참조"라는 텍스트**로 온다 (해당 공고의 주택형 3개 전부 그랬음).
  즉 이 카테고리는 **LH API만으로는 정확한 임대조건 금액을 알 수 없다** — 자금 시뮬레이터에서 이 값이 없으면
  "0원"이 아니라 "원문 공고 확인 필요"로 안내해야 한다 (D9과 같은 맥락). `HouseType.depositWon`/`monthlyRentWon`을
  `number | null`로 설계한 이유가 이것이다. `060`(공공임대 5·10년/분납임대)에서는 지금까지 항상 숫자로 왔다.
- `064`(가정어린이집)는 `CCR_CNNT_SYS_DS_CD` 값에 따라 응답 구조 자체가 갈린다는 게 문서상 확인됨 — 02일 때는 050/060 그룹과 같은
  구조, 03일 때는 061/062/063과 같은 구조로 추정되나 실호출로 검증 전까지 단정 안 함. 그래서 지금 어댑터는 064를 아예 다루지 않는다.
- `SPL_INF_TP_CD` 전체 코드는 활용가이드에 있음 (요약): `010` 토지 · `050`/`051`/`052` 분양주택(일반/분양전환/이익공유형) ·
  `060` 공공임대(5·10년, 분납) · `061` 임대주택 50년공공임대 · `062` 국민임대/장기전세/신축다세대/영구임대 · `063` 행복주택 ·
  `064` 가정어린이집 · `130`~`145` 매입임대·전세임대 각 유형(청년/신혼부부/다자녀/집주인리모델링/기숙사형/다가구/장기미임대/공공전세/임시사용) ·
  `220`~`224` 상가(입찰/추첨/공모심사) · `390` 신혼희망타운 · `999` 기타.

- **`SPL_INF_TP_CD=144`(든든전세주택 계열, 매입임대·전세임대 그룹) — 2026-09-01 실호출로 새로 확인.** 060/061/062/063과
  또 다른 세 번째 응답 모양이다: `dsList01`에 지역명·주택정보(단지명+주소)·주택형(면적 구간, 예: `"80㎡ 이상"`)·
  공급호수·모집인원이 **단지별로 여러 행**으로 온다 (한 공고에 여러 지역·여러 단지가 섞여 있는 매입임대 특성상 당연함).
  필드: `SGG_NM`(지역명, 시군구) · `DNG_HS_ADR`(주택정보 — 동+단지명) · `HTY_DS_NM`(주택형, 정확한 면적이 아니라
  **면적 구간 문자열**) · `LTR_SPL_RMNO`(공급호수) · `QUP_CNT`(모집인원). 실제 호출(경기북부 든든전세주택 공고)로
  사용자가 캡처한 마이홈포털 표(고양시 대화동 계림웨스트벨리 9/27, 하남시 신장동 쁘띠린 96/288 등)와 **값이 완전히 일치함을 확인** —
  즉 그 포털 화면의 데이터 출처가 바로 이 API라는 뜻이다.
  **새로 발견한 것 — 이 응답엔 `dsList01Nm`이라는, 필드명의 한글 라벨을 알려주는 보조 리스트가 같이 온다**
  (`{"HTY_DS_NM":"주택형","SGG_NM":"지역명","DNG_HS_ADR":"주택정보","LTR_SPL_RMNO":"공급호수","QUP_CNT":"모집인원"}`).
  060/061/062/063 응답에서는 이런 리스트를 본 적이 없다 — 브랜치마다 응답 봉투 자체가 이렇게 또 다르다는 뜻이므로
  144만 보고 130~143/145도 같은 모양일 거라고 단정하지 않는다 (7절 미확인 사항 참고).
  **이 API 응답에는 지도/홍보물 링크도, 실시간 신청건수(마이홈포털 표 하단 "신청건수: 현재 인터넷 청약신청자 현황")도 없다** —
  포털 화면의 지도·홍보물 버튼과 실시간 카운터는 이 3종 API의 데이터 범위 밖이다 (미확인이 아니라 이번 호출로
  "없다"는 것 자체를 확인함).

#### 매입임대·전세임대(130~145) 그룹 — 코드마다 필드가 또 다르다 (2026-09-02 실호출)
144 하나만 보고 "130~145는 다 이렇게 생겼겠지"라고 가정하면 안 된다는 게 실제로 증명됐다. 같은 UPP_AIS_TP_CD=13
목록에서 뽑은 서로 다른 5개 코드를 실호출한 결과, **최소 3가지 다른 응답 모양**이 나왔다:

| SPL_INF_TP_CD | 실제 공고 | 리스트 키 | 필드 | 면적 정보 |
|---|---|---|---|---|
| `130` (청년신혼부부매입임대리츠) | 인천지역 입주자 모집 | `dsList01` | `HTY_DS_NM`(주택유형)·`GNR_SPL_RMNO`(공급호수)·`LTR_UNT_NM`(상세지역=동+단지명)·`SBD_CNP_NM`(지역, 시/도만)·`PZWR_CNT`(당첨자수)·`CAL_QUP_CNT`(예비자수) | **있음, 정확한 값** — `"전용46㎡"`/`"전용59㎡"` 처럼 숫자를 바로 파싱 가능 |
| `131` (청년 매입임대주택) | 서울지역본부 예비입주자 모집 | `dsList02` (01 아님!) | `SX_PP_DS_NM`(전용여부)·`DNG_HS_ADR`(주택정보)·`LTR_SPL_RMNO`(공급호수)·`CNP_NM`(지자체명)·`QUP_CNT`(모집인원) | 없음 (필드 자체가 안 옴) |
| `132` (신혼·신생아매입임대주택Ⅱ) | 서울지역본부 예비입주자 모집 | `dsList01` | `SGG_NM`(지자체명)·`DNG_HS_ADR`(주택정보)·`LTR_SPL_RMNO`(공급호수)·`QUP_CNT`(모집인원) | 없음 |
| `141` (다자녀 매입임대주택) | 인천·부천 예비입주자 모집 | `dsList01` | 132와 동일 필드 | 없음 |
| `144` (든든전세주택) | 위 문단 참고 | `dsList01` | `HTY_DS_NM`(주택형=면적**구간**)·`SGG_NM`·`DNG_HS_ADR`·`LTR_SPL_RMNO`·`QUP_CNT` | **있음, 구간만** — `"80㎡ 이상"` 같은 버킷, 정확한 숫자 아님 |

즉 "단지명+주소"(`DNG_HS_ADR` 또는 `LTR_UNT_NM`)는 테스트한 5개 코드 전부(1315 제외)에서 어떤 이름으로든 존재해서
비교적 안정적이지만, **"면적"은 코드마다 있고 없고가 갈린다** — `130`은 정확한 ㎡ 숫자, `144`는 구간 라벨만,
`131`/`132`/`141`은 아예 필드 자체가 없다. `dsList01`이라는 같은 리스트 이름 안에서도(`132`/`141`/`144`) 어떤 건
면적 필드가 있고 어떤 건 없다 — 리스트 이름만 보고 필드 구성을 짐작하면 안 된다는 6절의 기존 경고가 이 그룹에도 그대로 적용된다.

**추가 이상 현상:** 목록 API가 특정 공고(`PAN_ID=2015122300020560`, "수원시 역세권 새빛 청년존")에 대해
`SPL_INF_TP_CD`를 `1315`라는, 문서에 없는 4자리 값으로 내려줬다. 이 값으로 공급정보 API를 호출하니 `dsList01`도
`dsList0X`도 전혀 없이 `resHeader`만 왔다(데이터 없음). 오타인지, `13`+`15`가 잘못 이어붙은 값인지, 진짜 존재하는
별도 코드인지 확인 안 됨 — 7절 미확인 사항으로 옮김. 이런 값이 수집기에 들어오면 예외 처리 없이 조용히 빈 배열로
넘어가게만 해두고, 강하게 실패시키지 않는 편이 안전하다.

**나머지 미호출 코드**(`133`/`135`/`137`~`140`/`142`/`143`/`145`)는 여전히 응답 모양을 모른다 — 실제로 그 유형의
공고가 수집될 때마다 하나씩 검증해서 늘려가야 한다 (062/063을 나중에 추가했던 것과 같은 방식).

#### 15057999 상세 API — URL·파라미터는 확보, 실호출 미검증
- 요청 파라미터는 공급정보 API와 동일 (`SPL_INF_TP_CD`+`CCR_CNNT_SYS_DS_CD`+`PAN_ID`+`UPP_AIS_TP_CD`+`AIS_TP_CD`).
- 응답도 `SPL_INF_TP_CD`별로 완전히 다른 구조 — `dsCtrtPlc`(접수처정보) / `dsSbd`(단지정보) / `dsAhflInfo`(첨부파일) / `dsEtcInfo`(기타정보, 정정사유·기타사항 등) /
  `dsEtcList`(신청자격, 매입·전세임대류에만 존재) 조합이며 브랜치마다 필드명이 다르다 (예: 국민임대 계열은 `LGDN_ADR`/`LGDN_DTL_ADR`, 공공분양 계열은 `LCT_ARA_ADR`/`LCT_ARA_DTL_ADR`로 주소 필드명 자체가 다름).
- **2026-08-25에 서로 다른 PAN_ID 2건으로 시도했으나 둘 다 `SERVICETIMEOUT_ERROR(05, 서비스 연결실패 에러)`.**
  같은 키·같은 URL 패턴으로 공급정보 API는 성공했으므로 우리 쪽 설정 문제는 아닌 것으로 보인다. LH 상세정보 백엔드의 일시적 장애로 추정 — 나중에 재시도 필요.

---

## 6.1 청약홈(한국부동산원) 분양정보 조회 API — 검증됨 (2026-08-25)

`15098547`. **LH와 게이트웨이 자체가 다르다** — `apis.data.go.kr`이 아니라 `api.odcloud.kr` (공공데이터포털이
3성급 이상 파일데이터를 자동 변환해 오픈API로 얹어주는 별도 시스템). 응답 봉투·인증 방식·페이지네이션 전부 LH와 다르므로
**LH 어댑터 코드를 재사용할 생각을 하지 말 것** — D4 소스 어댑터 패턴이 필요한 이유가 바로 이런 경우다.

### 인증
- Base: `https://api.odcloud.kr/api`
- 인증은 두 가지 방식이 스펙에 정의되어 있음: 헤더 `Authorization`(Infuser 방식) 또는 쿼리파라미터 `serviceKey`.
  **`serviceKey` 쿼리파라미터 방식으로 실제 호출 성공 확인함** (LH와 같은 계정 인증키, ENCODED 그대로 사용).

### 엔드포인트 (10개, 전부 `GET`)
| 경로 | 역할 |
|---|---|
| `getAPTLttotPblancDetail` | APT 분양정보 — 목록 검색 + 상세조회 겸용 |
| `getAPTLttotPblancMdl` | APT 주택형별 상세 (LH의 공급정보 API에 해당) |
| `getUrbtyOfctlLttotPblancDetail` / `...Mdl` | 오피스텔·도시형·민간임대·생활숙박시설 |
| `getRemndrLttotPblancDetail` / `...Mdl` | APT 무순위·잔여세대 |
| `getPblPvtRentLttotPblancDetail` / `...Mdl` | 공공지원 민간임대 |
| `getOPTLttotPblancDetail` / `...Mdl` | 임의공급 |

**LH처럼 목록/상세/공급정보 3분할이 아니다.** `*Detail` 엔드포인트 하나가 검색 필터(지역·주택명·모집공고일 범위)를
걸면 목록처럼, `HOUSE_MANAGE_NO`나 `PBLANC_NO`로 좁히면 상세처럼 동작한다 — 파라미터가 전부 `required: false`라
필터 조합은 자유. 주택형별 면적·가격만 별도로 `*Mdl` 엔드포인트에 있다 (LH의 공급정보 API와 같은 위치).

### 요청 파라미터 (`getAPTLttotPblancDetail` 기준, 전부 옵션)
`page`(기본 1) / `perPage`(기본 10) / `returnType`(JSON|XML) /
`cond[HOUSE_MANAGE_NO::EQ]` / `cond[PBLANC_NO::EQ]` / `cond[HOUSE_NM::LIKE]` /
`cond[HOUSE_SECD::EQ]`(01 APT, 09 민간사전청약, 10 신혼희망타운) /
`cond[HOUSE_DTL_SECD::EQ]`(01 민영, 03 국민) /
`cond[SUBSCRPT_AREA_CODE::EQ]` / `cond[SUBSCRPT_AREA_CODE_NM::EQ]` / `cond[HSSPLY_ADRES::LIKE]` /
`cond[RCRIT_PBLANC_DE::LT|LTE|GT|GTE]`(모집공고일 범위 — LH의 `PAN_ST_DT`/`PAN_ED_DT`에 해당하지만 필수가 아니다).
`getAPTLttotPblancMdl`도 필터는 **`Detail`과 똑같이 `cond[필드::EQ]` 형식이어야 실제로 걸린다** — 아래 정정 참고.

### 응답 봉투 — LH보다 훨씬 표준적
```json
{ "page": 1, "perPage": 10, "totalCount": 2853, "currentCount": 3, "matchCount": 2853,
  "data": [ { 공고... } ] }
```
최상위가 배열이 아니라 **객체**이고 `data`가 배열이다. LH의 `response[1].dsList` 언랩과는 다른 파싱 로직이 필요하다.

### 실제 확인된 응답 필드 (`getAPTLttotPblancDetail`, 2026-08-25 실호출)
`HOUSE_MANAGE_NO`/`PBLANC_NO`(공고 식별자, 이 API는 둘이 같은 값으로 옴) · `HOUSE_NM`(주택명) ·
`HOUSE_SECD_NM`(APT 등) · `HOUSE_DTL_SECD_NM`(민영/국민) · `RENT_SECD_NM`(분양주택 등) ·
`SUBSCRPT_AREA_CODE_NM`(지역명) · `HSSPLY_ADRES`(공급위치) · `TOT_SUPLY_HSHLDCO`(총공급세대수) ·
`RCRIT_PBLANC_DE`(모집공고일) · `RCEPT_BGNDE`/`RCEPT_ENDDE`(접수 시작/종료일) ·
`PRZWNER_PRESNATN_DE`(당첨자발표일) · `MVN_PREARNGE_YM`(입주예정월, `YYYYMM`) ·
`PBLANC_URL`(청약홈 원문 링크) · `CNSTRCT_ENTRPS_NM`(시공사) · `BSNS_MBY_NM`(사업주체) 등.

### `_AT` 플래그 필드 — 기술문서(`260129`)로 전부 확인됨
전부 `Y`/`N` 플래그. 값이 있으면 해당, `N`이면 비해당.
| 필드 | 의미 |
|---|---|
| `SPECLT_RDN_EARTH_AT` | 투기과열지구 |
| `MDAT_TRGET_AREA_SECD` | 조정대상지역 (`Y` 과열지역 / `N` 미대상주택) |
| `PARCPRC_ULS_AT` | 분양가상한제 |
| `IMPRMN_BSNS_AT` | 정비사업 |
| `PUBLIC_HOUSE_EARTH_AT` | 공공주택지구 |
| `LRSCL_BLDLND_AT` | 대규모 택지개발지구 |
| `NPLN_PRVOPR_PUBLIC_HOUSE_AT` | 수도권 내 민영 공공주택지구 |
| `PUBLIC_HOUSE_SPCLW_APPLC_AT` | 공공주택 특별법 적용 여부 |

**공공주택 판정 규칙(문서에 명시):** `HOUSE_DTL_SECD='03'`(국민) & `PUBLIC_HOUSE_SPCLW_APPLC_AT='Y'` 인 경우에만 "공공주택"이며,
이때만 `getAPTLttotPblancMdl` 응답의 특별공급-청년(`YGMN_HSHLDCO`)·특별공급-신생아(`NWBB_HSHLDCO`) 세대수가 의미 있는 값을 가진다.
이 조건을 안 걸고 두 필드를 그냥 읽으면 민영주택에서는 항상 0으로 나와 잘못된 결론을 낼 수 있다.

### 코드값 (기술문서 확보)
```
SUBSCRPT_AREA_CODE  서울100 · 강원200 · 대전300 · 충남312 · 세종338 · 충북360 · 인천400 · 경기410
                    광주500 · 전남513 · 전북560 · 부산600 · 경남621 · 울산680 · 제주690 · 대구700 · 경북712
                    → 전국 또는 광역권(예: 서울·경기·인천 통합공고) 조건이면 지역코드 자체가 없음(빈 값)

HOUSE_SECD(APT)     01 APT / 09 민간사전청약 / 10 신혼희망타운
HOUSE_DTL_SECD(APT) 01 민영 / 03 국민
RENT_SECD           0 분양주택 / 1 분양전환 가능임대

SEARCH_HOUSE_SECD   (오피스텔/도시형/민간임대/생숙, 검색조건용)
                    0201 도시형생활주택 / 0202 오피스텔 / 0203 민간임대 / 0204 생활형숙박시설

HOUSE_SECD(잔여세대) 04 무순위 / 06 불법행위 재공급
```

### Mdl(주택형별) 엔드포인트마다 면적 필드가 다르다 — 중요
5종 `*Mdl` 엔드포인트가 면적을 주는 방식이 전부 다르다. `exclusiveAreaM2`/`supplyAreaM2`로 정규화할 때 엔드포인트별로 분기해야 한다.
| 엔드포인트 | 면적 관련 필드 | 비고 |
|---|---|---|
| `getAPTLttotPblancMdl` | `HOUSE_TY`(전용면적, 예: `"060.9495A"` → 60.95㎡) + `SUPLY_AR`(공급면적) | 기술문서 예제 6건엔 `SUPLY_AR`이 빠져있었지만 **우리가 직접 호출(2026-08-25, 검암역 푸르지오 프라베뉴)해서 실제로 존재함을 확인함** — 문서 예제 누락이었을 뿐 |
| `getUrbtyOfctlLttotPblancMdl` | `EXCLUSE_AR`(전용면적) 명시 필드로 옴 | `HOUSE_TY` 없음, `TP`(타입)만 있음 |
| `getRemndrLttotPblancMdl` | `HOUSE_TY`만, `SUPLY_AR`은 실제 예제에서 `null` | 전용면적은 `HOUSE_TY` 파싱 필요 |
| `getPblPvtRentLttotPblancMdl` | `EXCLUSE_AR` + `SUPLY_AR` + `CNTRCT_AR`(계약면적) 전부 명시 필드로 옴 | 가장 완전한 형태 |
| `getOPTLttotPblancMdl` | `HOUSE_TY`만 | |

`getAPTLttotPblancMdl` / `getPblPvtRentLttotPblancMdl` 은 특별공급 유형별 세대수(다자녀·신혼부부·생애최초·노부모부양·기관추천·청년·신생아 등)까지
내려줘서, 자격 판정 엔진에서 "이 주택형에 내가 지원 가능한 특별공급이 몇 세대 배정됐는지" 계산에 바로 쓸 수 있다 — LH 공급정보 API에는 없는 데이터.

### 에러 코드 체계가 LH와 완전히 다르다
LH는 `OpenAPI_ServiceResponse.cmmMsgHeader.returnReasonCode`(0~99, 30여 개 세분화된 코드)를 쓰는데,
청약홈(odcloud) API는 훨씬 단순한 HTTP 스타일이다: **`200` 성공 / `401` 인증 정보 부정확 / `500` 서버 오류**. 세 가지뿐.
어댑터의 에러 처리 로직을 LH와 절대 공유하지 말 것 — 이것도 D4가 필요한 이유.

### 공공지원 민간임대(`getPblPvtRentLttotPblancMdl`) — 2026-09-02 실호출로 처음 확인
LH가 국민임대·행복주택에서 "공고문 참조"로 숨기는 것과 달리, **이 상품(청약홈의 공공지원 민간임대)은
주택형별로 숫자 금액을 실제로 준다.** 의왕청계역 시티프라디움(`HOUSE_MANAGE_NO=2026850045`)로 실호출한
결과, 주택형(`TP`)마다 `SUPLY_AMOUNT` 필드에 `"60500"`/`"42770"`/`"61100"`/`"69500"`/`"69900"` 같은
숫자가 왔다. `EXCLUSE_AR`(전용면적)·`SUPLY_AR`(공급면적)·`CNTRCT_AR`(계약면적)도 명시 필드로 같이 온다
(6.1절 Mdl 표에 이미 기록된 내용과 일치).

**미확정 — 단정하지 않는다:** `SUPLY_AMOUNT`의 정확한 한글 원어와 단위(만원/천원/원). 이 API는 LH의
`dsList01Nm` 같은 필드 라벨 힌트를 안 준다. 이 상품이 순수 임대(공공지원 민간임대는 분양이 아니라
전세형 임대)라는 점, 그리고 별도의 월임대료 필드가 안 보인다는 점에서 "임대보증금(전세보증금)"일
가능성이 높다고 추정하지만, 공식 문서 문구 없이 `depositWon`으로 확정 매핑하지 않는다 — 6.1절
"가격 필드" 항목의 `LTTOT_TOP_AMOUNT`와 같은 처지다. 월임대료에 해당하는 필드가 정말 없는지(순수
전세형이라 그런 건지, 이 공고 표본 하나만 그런 건지)도 미확인.

이걸로 "가격을 API로 알 수 있는 범위"가 하나 더 늘었다: LH 060그룹 / 청약홈 APT(`LTTOT_TOP_AMOUNT`) /
청약홈 공공지원민간임대(`SUPLY_AMOUNT`) — 셋 다 필드 의미·단위는 미확정이지만 실제 숫자는 온다.
반대로 LH 061/062/063(국민임대·행복주택)과 LH 매입임대·전세임대(130~145)는 API 자체에 가격 필드가
없거나 텍스트로만 온다 — 이건 우리 구현 문제가 아니라 데이터 자체가 없는 것이다.

### 기타 확인된 사실
- 데이터 갱신주기: **매일**.
- 서비스 제공자: 한국부동산원 ICT센터 ICT전략부.
- `getAPTLttotPblancDetail` 요청 파라미터는 문서상 `RCRIT_PBLANC_DE` 날짜 형식이 `YYYY-MM-DD`(하이픈)인데, `getPblPvtRentLttotPblancDetail`·`getOPTLttotPblancDetail`은 `YYYYMMDD`(하이픈 없음)로 **엔드포인트마다 날짜 포맷이 다르다.** 어댑터에서 이 차이를 흡수해야 한다.

### 가격 필드 — 2026-09-01 실호출로 처음 확인
`getAPTLttotPblancDetail`(목록/상세) 응답에는 **가격 필드가 없다** — 실제 공고(남양주진접2 A-4블록 신혼희망타운,
`HOUSE_MANAGE_NO=2026820009`) 1건을 실호출해서 응답 전체 필드를 확인했고, 분양가·공급금액 계열 필드가
단 하나도 없음을 확인했다 (필드 목록은 위 "실제 확인된 응답 필드" 참고 — 그게 전부다).

가격은 **`getAPTLttotPblancMdl`(주택형별) 쪽에 `LTTOT_TOP_AMOUNT`** 라는 필드로 실제로 온다.
같은 공고(`HOUSE_MANAGE_NO=2026820009`, 신혼희망타운·공공분양, 주택형 `055.0000O`=전용 55㎡대, 공급면적 78.45㎡)로
실호출해서 `"LTTOT_TOP_AMOUNT": "38446"` 값을 실제로 받았다. 이 필드는 **기존에 어댑터 코드가 다루지 않고 있던 필드다**
(6.1절 "Mdl 엔드포인트마다 면적 필드가 다르다" 표는 면적만 다뤘음 — 가격은 그때 확인 안 됨).

**미확정 사항 (7절로도 옮김):**
- `LTTOT_TOP_AMOUNT`의 정확한 한글 원어와 의미 — 필드명과 가져온 값의 크기(수만 단위)로 볼 때 "분양(최고)금액" 계열로
  추정되지만, 이걸 명시한 공식 문서 문구를 아직 확인 못했다. 어댑터에 반영하기 전에 청약홈 기술문서에서 정확한 정의를
  확인할 것 — 그때까지 `supplyPriceWon`으로 단정 매핑하지 않는다.
- 단위(만원 vs 천원 vs 원) — LH처럼 원 단위인지, 청약홈 관행상 만원 단위인지 문서로 확인 안 됨. 잘못 짐작하면
  D5/절대 규칙 위반("확인되지 않은 외부 데이터를 상상하지 않는다")이 되므로, 확인 전까지 코드에 배율을 넣지 않는다.
- 다른 4종 `*Mdl` 엔드포인트(오피스텔·잔여세대·공공지원민간임대·임의공급)에도 같은 이름/역할의 가격 필드가 있는지 — 아직 미호출.
- 임대전환 가능임대(`RENT_SECD=1`) 공고에서도 이 필드가 채워지는지, 채워진다면 분양가와 임대보증금 중 어느 쪽 의미인지.

**부수 발견 — `getAPTLttotPblancMdl` 필터 파라미터 문서 오류 정정:** 이전에 "`page`/`perPage`/`returnType`/
`HOUSE_MANAGE_NO`/`PBLANC_NO`만 지원"이라고 적어뒀던 것은 **파라미터 이름만 맞고 형식이 틀렸다.** 실제로 bare
`HOUSE_MANAGE_NO=2026820009` 형태로 호출하면 서버가 이 값을 무시하고 전체 14,614건을 그대로 반환한다
(`matchCount`가 필터 안 걸릴 때와 동일하게 유지됨 — 실호출로 확인). `Detail`과 똑같이
**`cond[HOUSE_MANAGE_NO::EQ]=2026820009` 형식**으로 호출해야 실제로 `matchCount=1`로 줄어든다 — 이것도 실호출로 확인.
공급정보 수집기를 만들 때 이 형식을 안 쓰면 전체 데이터를 매번 훑는 코드가 된다.

---

## 7. 미확인 사항 — 추측 금지

아래는 문서에 없다. **실제 호출로 검증하기 전까지 전제로 삼지 말 것.**

- 조회 기간 상한 (초기값 2개월인데 최대 몇 개월까지 가능한지)
- `PG_SZ` 최댓값
- 결과 0건일 때 응답 형태 (`dsList`가 빈 배열인지, 키 자체가 없는지)
- 지역코드 다중 지정 가능 여부 (수도권 3개를 한 번에? 지역별 따로?)
- 에러 시 `SS_CODE` 값 (정상이 `Y`인 것만 확인됨. 단 `SERVICETIMEOUT_ERROR` 등 상위 에러는 `SS_CODE` 자체가 없는 다른 응답 봉투로 온다는 것은 확인됨 — 아래 참고)
- 15057999(`lhLeaseNoticeDtlInfo1`) **실제 응답 — 여전히 미검증.** URL·파라미터는 활용가이드로 확보했으나 실호출 2회 모두
  `SERVICETIMEOUT_ERROR(05)`로 실패(2026-08-25). LH 백엔드 일시 장애로 추정되나, 재시도해서 실제로 성공 응답을 받기 전까지는
  6절에 옮겨 적은 "문서상" 필드 구조(`dsCtrtPlc`/`dsSbd`/`dsAhflInfo`/`dsEtcInfo`/`dsEtcList`)를 실제 동작으로 단정하지 않는다.
- **`dsList04`(15056765 공급정보 API, `SPL_INF_TP_CD=060` 응답)의 용도** — 활용가이드에 없는 리스트가 실제 응답에 나타남.
  필드 구성은 `dsList02`(공공임대)와 동일. 5년/10년 공공임대 구분일 가능성이 있으나 근거 문서 없음.
- ~~`SPL_INF_TP_CD=130`~`143`/`145`도 144와 같은 응답 모양인지~~ **2026-09-02 실호출로 확인 완료 — 같지 않다.**
  6절 "매입임대·전세임대(130~145) 그룹 — 코드마다 필드가 또 다르다" 참고. 130/131/132/141/144 다섯 개를 실제로 호출해서
  최소 3가지 다른 응답 모양을 확인했다. 나머지 미호출 코드(133/135/137~140/142/143/145)는 여전히 미확인.
- **목록 API가 내려준 `SPL_INF_TP_CD` 값 `1315`의 정체** — 3자리 코드 체계에 없는 4자리 값(수원시 역세권 새빛
  청년존 공고). 오타/이어붙임/실재하는 별도 코드 중 뭔지 확인 안 됨. 이 값으로 공급정보 API를 호출하면 데이터 없이
  `resHeader`만 온다 (6절 참고).
- 공공데이터포털 일일 트래픽 실제 한도 (마이페이지에서 확인)
- `PAN_SS` 상태값의 정확한 전체 목록 (지금까지 실제 응답에서는 "공고중"만 관측됨)

**청약홈(15098547) 관련**
- `page`/`perPage`의 상한값 (기술문서에 명시 없음)
- `HOUSE_SECD::EQ`에 `09`(민간사전청약)·`10`(신혼희망타운)을 실제로 넣었을 때의 동작 (지금까지는 필터 없이 호출해서 `01`(APT) 위주로만 받음)
- 결과 0건일 때 응답 형태 (`data: []`로 오는지 다른 형태인지) — 기술문서 예제는 전부 1건 이상만 보여줌
- 10개 엔드포인트 중 `getAPTLttotPblancDetail`/`getAPTLttotPblancMdl` 2개만 실제로 호출해봤다. 나머지 8개(오피스텔·잔여세대·공공지원민간임대·임의공급 각 Detail/Mdl)는
  기술문서 스펙과 예제 JSON만 확인했고 **실제 호출은 안 해봤다** — 문서 예제와 실제 응답이 다를 수 있다는 게 이미 한 번 확인됐다(`SUPLY_AR`).
- `getAPTLttotPblancMdl`의 `LTTOT_TOP_AMOUNT` 필드의 정확한 정의와 단위(만원/천원/원) — 6.1절 "가격 필드" 참고.
  실제 값은 받았지만(`"38446"`) 공식 문서 근거 없이 의미를 확정하지 않는다.
- `getPblPvtRentLttotPblancMdl`의 `SUPLY_AMOUNT` 필드도 마찬가지 — 임대보증금으로 추정되나 확정 문서 없음.
  이 상품에 월임대료 필드가 정말 없는지도 미확인(표본 1건만 확인) — 6.1절 "공공지원 민간임대" 참고.
- 나머지 4종 `*Mdl` 엔드포인트에도 같은 이름/역할의 가격 필드가 있는지, `RENT_SECD=1`(분양전환 임대) 공고에서
  `LTTOT_TOP_AMOUNT`가 채워지는지와 그 경우 의미.

---

## 8. 데이터 소스 현황

| 기관 | 공고 자동 수집 | 상태 |
|---|---|---|
| LH | 가능 | 3종 세트. **1차 대상**. 인증키 발급·목록/공급정보 실호출 성공 (2026-08-25) |
| 청약홈 (한국부동산원) | 가능 | `한국부동산원_청약홈 분양정보 조회 서비스`(15098547) — Swagger 스펙 확보 + 실호출 성공 (2026-08-25). 상세는 6.1절. 2차 |
| GH | 조건부 | 파일데이터만. 갱신주기 확인 필요 |
| SH | 못 찾음 | 웹 검색 재확인(2026-08-25, 2026-09-16) — "청년매입임대주택 공개 주택목록_20211231" 같은 날짜 박힌 파일데이터만 나옴. 살아있는 공고 조회 오픈API는 못 찾음 (미개방인지 금지된 아님) |
| HUG 든든전세 | 못 찾음 | 공고 API 미발견 |

### 파일데이터 ≠ 오픈API
포털 목록에서 둘 다 "오픈API"로 보이지만, 파일데이터는 CSV 스냅샷을 나중에 변환한 것이라
**갱신이 멈춰 있다.** 데이터셋 이름에 `_20250821` 같은 날짜가 붙어 있으면 의심할 것.
"현재 모집 중인 공고"를 파일데이터로는 못 만든다.

**판별법 하나 더 (2026-09-16 확인) — URL 자체가 다르다.** 검색 결과 URL이 `data.go.kr/data/{id}/fileData.do`면
파일데이터, `openapi.do`(또는 활용신청 방식)면 오픈API다. 화면 UI로도 구분 가능: 파일데이터 카드는
"CSV/JSON+XML" 포맷 태그 + **"다운로드" 버튼**이 바로 붙어있고, 오픈API는 "활용신청" 버튼 → API 명세 페이지로
이어진다. 사용자가 공공데이터포털 검색 결과 캡처(서울주택도시공사_청년매입임대주택 당첨자 및 예비자 커트라인 /
공공주택 현황 / 주택분양 정보, 전부 "다운로드" 버튼 있는 카드)를 보여줘서 확인한 것 — 첫 번째를 웹검색으로
찾아보니 실제 URL이 `data.go.kr/data/15102879/fileData.do`, 정식 제목은
**"서울주택도시공사_청년매입임대주택 당첨자 및 예비자 커트라인_20211230"**로 2021년 스냅샷이었다.
게다가 내용 자체가 "당첨자·예비자 커트라인"(이미 끝난 추첨 결과 점수)이라 성격상으로도 "지금 접수 중인 공고
목록"과 다르다. **비슷한 유형이 매번 새 날짜를 붙여 별개 데이터셋으로 재발행되는 패턴**도 확인함
(`...당첨 커트라인_20211230`과 `신혼부부신생아 매입임대 당첨 커트라인_20250711`이 서로 다른 dataId) —
"주기적으로 갱신되는 하나의 API"가 아니라 "매번 새로 올라오는 스냅샷 파일"이라는 뜻.
"공공주택 현황"·"주택분양 정보" 두 개는 카드 UI(다운로드 버튼)로 봐선 같은 파일데이터로 추정되지만
정확한 URL은 못 찾아서 100% 확정은 아니다 — 직접 URL을 받으면 재확인.

### 마이홈포털을 복제할 수 없는 이유
마이홈포털(myhome.go.kr)은 LH가 위탁 운영하며 **기관 간 연계**로 데이터를 받는다.
민간 개발자에게 열린 통로가 아니고, 사이트는 robots.txt로 자동 접근을 막고 있다.
"저 사이트가 하니까 우리도 되겠지"는 위험한 가정이다.

---

## 9. 실거래가 API 관련 (2단계에서 사용)

국토교통부 아파트 매매 실거래가 API (data.go.kr/data/15126469)

- 응답이 **XML**. 지역코드 5자리 + 계약년월 단위 조회.
- **거래 고유 ID가 없다.** 재수집 시 중복을 막으려면
  `sggCd + umdNm + aptNm + jibun + excluUseAr + 계약일 + dealAmount + floor` 해시(`dealKey`)로
  유니크 인덱스를 건다.
- `dealAmount`는 `"12,000"` 같은 콤마 포함 문자열, 단위 만원.
- `cdealType`(해제 여부)이 채워진 건은 **취소된 거래**다. 시세 통계에서 제외하거나 별도 표시.
- 갱신 정책 (월 = 지역코드 + 계약월):
  | 월 상태 | 조건 | 정책 |
  |---|---|---|
  | 미확정 | 계약월이 최근 3개월 이내 | 1일 1회 재수집, **월 전체 교체** |
  | 확정 | 그보다 오래됨 | 최초 1회 수집 후 동결 |

  신고 지연으로 최근 월은 건수가 계속 늘고, 해제·정정으로 기존 건이 바뀐다.
  upsert가 아니라 전체 교체인 이유: 금액이 바뀌면 `dealKey`가 달라져 유령 문서가 남는다.
- 서비스 범위는 **수도권** (서울 25 + 인천 10 + 경기 31 → 66개 시군구).
  5년치 → 약 4,000회 호출로 하루 안에 적재 가능. 전국은 15,000회.

### 인증키 두 종류
공공데이터포털은 Encoding / Decoding 키를 발급하는데 **API마다 하나만 동작하는 경우가 있다.**
`.env`에 둘 다 넣고 `DATA_GO_KR_KEY_MODE`로 선택. 30번 에러가 나면 모드를 바꿔 확인.

**LH 3종 세트는 ENCODED 모드로 실제 성공 확인함(2026-08-25)** — URL 쿼리스트링에 `%2F`/`%2B`/`%3D` 형태 그대로 넣어서 호출 성공.
`server/.env`의 `DATA_GO_KR_KEY_MODE`를 `ENCODED`로 맞춰뒀다. 국토교통부 실거래가 API(9절)는 아직 미확인이라 그쪽은 별도 확인 필요.

---

## 10. 현재 진행 상태

### 완료
- 요구사항 정의서 v0.1 (`docs/requirements.md` — 웹 대화 산출물, 아직 이 저장소에 없음)
- LH 목록 API 명세 분석
- **프로젝트 스캐폴딩 (2026-08-19, 로컬 디스크에 실제 생성)**
  - `client/` Vite + React 19 + TS + TanStack Query + Tailwind v4
  - `server/` Express 5 + Mongoose + tsx, `/api/health` 동작 확인
  - 응답 봉투(`common/response.ts`), 에러 계층(`common/errors.ts`), 에러 핸들러
  - D2 경계 표시용 `server/src/ingest/README.md`
  - Vite 프록시 `/api` → 4000 확인
- **공고 리스트/상세 화면 와이어프레임 (2026-08-25, Claude Design 캔버스)** — 예시 데이터 기반 클릭 가능 프로토타입.
  지역·거래유형·건물유형·상태 체크박스 다중선택 + 검색 + 페이지네이션, 상세 화면 타임라인·주택형 표.
  스타일은 "따뜻한 친근형"(아이보리+코랄+Gowun Dodum)으로 확정.
  **건물유형(아파트/빌라·다세대/오피스텔/단독주택)은 화면 설계 중 임의로 추가한 카테고리로, LH API에 검증된 필드가 아니다** — 7절 미확인 사항 참고.
- **공공데이터포털 인증키 발급 + LH 3종 세트 활용가이드 확보 + 목록·공급정보 API 실호출 성공 (2026-08-25)**
  `lhLeaseNoticeSplInfo1`(15056765) / `lhLeaseNoticeDtlInfo1`(15057999) / `lhLeaseNoticeInfo1`(15058530, 목록) 3종 모두 활용신청 승인, 같은 일반인증키(ENCODED 모드) 사용 확인.
  `server/.env`에 저장(커밋 대상 아님).
  - 목록 API: 문서화된 URL 패턴대로 실호출 성공, 6절 응답 구조와 실제 응답 필드 전부 일치 확인. `SPL_INF_TP_CD`가 응답에 실제로 존재하는 것도 확인(연결 키가 4개가 아니라 5개였음 — 6절 정정 참고).
  - 공급정보 API(15056765): 상세/공급정보 활용가이드 PDF 확보 후 정확한 URL(`.../lhLeaseNoticeSplInfo1/getLeaseNoticeSplInfo1`)로 실호출 성공. 김포한강 Ac-05 공공임대 공고로 주택형별 전용면적·공급면적·임대보증금·월임대료 실데이터 수신 확인.
  - 상세 API(15057999): URL·파라미터는 활용가이드로 확보했으나 서로 다른 PAN_ID 2건 모두 `SERVICETIMEOUT_ERROR`로 실패 — LH 백엔드 쪽 문제로 추정, 재시도 필요 (6·7절 참고).
  - 에러 응답이 정상 응답과 완전히 다른 봉투(`OpenAPI_ServiceResponse.cmmMsgHeader`)로 온다는 것도 실제로 확인 (6절).
- **청약홈(15098547) API 스펙 확보 + 실호출 성공 (2026-08-25)** — Swagger 문서(`infuser.odcloud.kr`)로 정확한 엔드포인트·파라미터 확인 후
  `getAPTLttotPblancDetail` 실호출 성공 (인천 검암역 푸르지오 프라베뉴 등 실제 APT 분양공고 3건 수신, 전체 2,853건). 상세는 6.1절.
  **LH와 게이트웨이·응답 구조가 완전히 다르다** — 어댑터를 공유할 수 없고 D4 패턴대로 독립적으로 만들어야 한다.
- **청약홈 기술문서(`260129`) 확보 + `getAPTLttotPblancMdl` 실호출로 필드 재검증 (2026-08-25)**
  `_AT` 플래그 8종 전부 뜻 확인 (투기과열지구·조정대상지역·분양가상한제·정비사업 등, 6.1절). 지역코드·주택구분코드 등 전체 코드표 확보.
  에러 코드가 `200`/`401`/`500` 세 가지뿐으로 LH와 완전히 다름을 확인. Mdl(주택형별) 엔드포인트 5종의 면적 필드 구성이 서로 다르다는 것도 확인 —
  `HOUSE_TY` 문자열 파싱이 필요한 것과 `EXCLUSE_AR`/`SUPLY_AR` 명시 필드가 오는 것이 섞여있다. `getAPTLttotPblancMdl`의 `SUPLY_AR`은 문서 예제엔
  없었지만 직접 호출해서 실존 확인함.
- **MongoDB 연결 완료 (2026-08-25)** — 로컬 mongod 대신 **MongoDB Atlas 무료 클러스터(M0, AWS ap-northeast-2)** 사용.
  `server/.env`의 `MONGO_URI`를 Atlas 연결 문자열로 교체(Drivers 탭에서 받은 문자열에 DB 이름 `/realestate`을 직접 추가해야 했음 — 기본 제공 문자열엔 없음).
  서버 기동 후 `/api/health` 응답이 `db: "disconnected"` → `db: "connected"`로 바뀐 것 실제 확인.
- **LH 원본 타입 → 언랩 → 코드사전 → 날짜파싱 → Announcement/HouseType 타입 + 어댑터 (2026-08-27, 로드맵 1~5단계 완료)**
  `server/src/ingest/lh/`(raw-types·unwrap·codes·dates·client·collector·scheduler·adapter)와 `server/src/modules/announcements/`(타입·Mongoose 모델)로 구현.
  실제 원본 JSON을 그대로 먹여서 어댑터 동작을 실행까지 확인함(타입체크만이 아니라).
  - `sourceKey`를 LH 전용 5개 필드 타입으로 고정했다가, 청약홈처럼 키 모양이 다른 소스가 못 들어가는 걸 스키마 작업 직전에 발견해서
    `Record<string, string>`(Mixed)으로 일반화함 — D4를 지키려면 공통 타입이 특정 소스 모양에 묶이면 안 된다.
  - 가격 필드는 원래 "만원 단위 정수"로 설계했었는데, LH 임대보증금이 만원 단위로 안 떨어지는 실제 값(예: 68,801,000원)으로 와서
    **원 단위 정수로 정정**(5절). `depositManwon`→`depositWon` 등 필드명도 변경.
  - **국민임대·행복주택(`SPL_INF_TP_CD` 062/063) 계열은 임대보증금·월세를 숫자로 안 주고 "공고문 참조" 텍스트로 준다** — 실제 호출로 확인(6절).
    `HouseType.depositWon`/`monthlyRentWon`을 `number | null`로 설계한 이유. 자금 시뮬레이터가 이 값을 "0원"으로 오해하면 안 된다.
- **Mongoose 스키마 + 인덱스 (2026-08-27, 로드맵 6단계)** — `AnnouncementModel`(source+sourceKey.panId 유니크 인덱스)과
  `HouseTypeModel`(source+panId+complexName+houseType 유니크 인덱스). 실제 Atlas에 저장 → 같은 panId로 재수집(upsert) →
  중복 안 생기고 같은 문서가 갱신되는 것까지 실행해서 확인함.
- **수집기 + 스케줄러 (2026-08-27, 로드맵 7단계)** — 서울·인천·경기(11절 — 수도권 한정) × 기간으로 LH 목록 API를 훑어서
  지원 유형(05/06/13/39)만 저장하고, `SPL_INF_TP_CD`가 060/061/062/063인 것만 공급정보(주택형)까지 받아온다.
  실제 호출로 검증(2026-08-20~27, 좁은 기간): 30건 중 16건 제외 필터링, 14건 저장, 주택형 33건 저장, 에러 0건.
  매일 오전 9시(항상 KST 고정, 서버가 실제로 어느 리전에 떠 있든 무관하게 계산) 자동 실행 — `server/src/index.ts`에 연결.
  놓친 회차는 그냥 건너뛴다(재시도·보정 로직 없음) — v1 범위 밖으로 유예.
  `064`(가정어린이집)는 `CCR_CNNT_SYS_DS_CD`에 따라 응답 구조가 갈리는 것으로 보여 아직 다루지 않음 (6.1/7절).
- **조회 API (2026-08-27, 로드맵 8단계)** — `GET /api/announcements`(지역·거래유형·상태·검색어·페이지네이션),
  `GET /api/announcements/:id`(상세 + 주택형 목록). `announcement.service.ts`는 D3대로 req/res를 모른다.
  D5를 실제로 구현: DB에는 `postedAt`/`noticeDate`/`closingAt`만 있고, 상태(`open`/`closed`)는 매 요청마다
  `closingAt`과 현재시각을 비교해서 계산 — 저장 안 함. 실제 서버 기동 후 목록·지역필터·검색어·상세·
  존재하지 않는 id(404)·잘못된 형식의 id(404, CastError로 안 새게 사전 검증)까지 실제 호출로 확인함.
  **LH 목록 API에 접수시작일이 없어서 상태는 2단계(open/closed)만 계산 가능** — D5 참고.
- **공고 리스트/상세 화면 실데이터 연동 (2026-09-01, 로드맵 9단계)** — 와이어프레임(Option A, 따뜻한 친근형)을
  실제 React 컴포넌트로 옮기고 `/api/announcements`·`/api/announcements/:id`에 연결.
  `client/src/features/announcements/`에 `types.ts`(서버 타입 수동 미러링, `sourceKey`·`source` 등 화면에
  안 쓰는 필드는 의도적으로 제외) · `api.ts`(`useAnnouncements`/`useAnnouncementDetail` TanStack Query 훅) ·
  `constants.ts`(필터 옵션 — DB에 실제로 존재하는 `regionName`/`categoryCode` 값을 스크래치 스크립트로
  직접 조회해서 만듦, 추측 아님) · `format.ts`(`formatDate`, `formatWon` — null은 "원문 공고 참조") ·
  `AnnouncementListPage.tsx`(사이드바 체크박스 필터 + 검색 + 카드 리스트 + 페이지네이션) ·
  `AnnouncementDetailPage.tsx`(타임라인 + 주택형 표 + 원문 링크 버튼)로 구현.
  `react-router-dom` 신규 설치, `main.tsx`에 `BrowserRouter` 추가, `App.tsx`를 라우팅 전용으로 교체
  (기존 헬스체크 확인용 화면은 제거됨 — `useHealth.ts` 훅 자체는 남아있으나 더 이상 화면에서 안 씀).
  디자인 토큰(아이보리 배경·코랄 accent·Gowun Dodum 제목 폰트)은 `.design/list-detail-wireframe/`의
  승인된 캔버스 HTML에서 그대로 가져와 `client/src/index.css`의 CSS 변수로 옮김; `client/index.html`에
  Google Fonts 링크 추가.
  클라이언트 쪽 다중선택 필터를 위해 `client/src/lib/api.ts`의 쿼리 빌더를 `URLSearchParams.set` →
  `.append` 루프로 변경(배열 값 지원, Express의 반복 키=배열 관례에 맞춤).
  **실제 서버(Atlas 연결)+클라이언트(Vite) 둘 다 띄운 상태에서 Vite 프록시를 통해 목록/상세 API를 curl로
  실호출 확인** — 상세 응답에서 서울번동3 행복주택(063)의 `depositWon`/`monthlyRentWon`이 실제로 `null`로
  내려오는 것까지 확인해서 "원문 공고 참조" 표시 경로가 실데이터로 실제 동작함을 검증.
  타입체크(`tsc -b --noEmit`) 통과, Vite가 새 모듈들을 컴파일 에러 없이 서빙하는 것도 확인.
  **다만 이 환경에는 브라우저 자동화 도구가 없어 렌더링된 화면을 직접 눈으로 보지는 못했다** — 사용자가
  브라우저로 직접 확인 필요.

- **매입임대·전세임대(LH SPL_INF_TP_CD 130/131/132/141/144) 공급 정보 지원 추가 (2026-09-02)** — 사용자가
  마이홈포털의 "모집세대" 표(지역명·주택정보·주택형·공급호수·모집인원) 캡처를 보여주며 이런 형태의 구현이
  가능한지 물어서 실호출로 조사 → 5개 코드를 직접 호출해 검증 → 어댑터 구현까지 진행.
  `HouseType`과 별개로 `PurchaseLeaseSupply` 타입 신설(6절/D6과 같은 이유 — 한 공고가 여러 시/군/구에
  흩어진 여러 단지를 동시에 모집하는 구조라 "한 단지 안의 주택형"과 성격이 다름). `exclusiveAreaM2`는
  130(정확한 값)만 채워지고 131/132/141/144는 null(면적 필드 자체가 없거나 구간 라벨만 있음) — `areaLabel`에
  원문 라벨을 그대로 보존해서 화면에선 있는 그대로 보여준다.
  `server/src/ingest/lh/`에 raw-types·unwrap·adapter 4벌 추가, `client.ts`의 `fetchLhSupplyApartment`를
  `fetchLhSupply`로 일반화(요청 파라미터가 SPL_INF_TP_CD와 무관하게 동일해서 061/062/063과 130/131/132/141/144가
  같은 함수를 씀). `PurchaseLeaseSupplyModel` 신설 — 안정적인 자연키가 없어서(같은 단지가 중복 등장 가능)
  9절 실거래가 정책과 같은 이유로 upsert 대신 `panId` 단위 delete+insert로 매번 통째로 교체.
  `announcement.service.ts`의 `getAnnouncementDetail`이 `HouseTypeModel`과 나란히 조회해서
  `AnnouncementDetail.purchaseLeaseSupplies`로 함께 내려준다. 클라이언트 상세 화면은 `houseTypes`가
  있으면 기존 "주택형별 공급 정보" 표를, 없고 `purchaseLeaseSupplies`가 있으면 "모집세대" 표를 보여준다.
  **실제 DB에 있는 든든전세주택 공고(144)로 실제 API 응답을 검증** — 지역명·단지명·면적구간·공급호수·모집인원이
  사용자가 캡처한 마이홈포털 화면의 "합계 134 / 402"까지 숫자 하나 안 틀리고 정확히 재현됨을 확인.
  130/131/132는 DB에 아직 해당 공고가 없어서 라이브 API 호출로만 검증(테스트 후 정리, DB엔 안 남김).
  **지도/홍보물 링크, 실시간 신청건수 카운터는 이 API 범위 밖 — 구현 안 함** (사용자에게 설명하고 동의받음).
  **범위 제한 — 매입임대·전세임대 전체(130~145, 15개 코드) 중 5개만 지원.** 나머지(133/135/137~140/142/143/145)는
  아직 응답 모양 미확인 — 6·7절 참고. 수집기가 이 코드들을 만나면 `skippedHouseTypeUnsupported`로 건너뛴다
  (062/063 때와 같은 패턴 — 실제 공고가 걸릴 때마다 하나씩 검증해서 추가).

- **가격 직접 입력 UI (2026-09-02, 자금 시뮬레이터 로드맵 1단계)** — API가 가격을 안 주는 경우가
  생각보다 넓다는 걸(D9 참고) 확인한 뒤, 계산 로직보다 "가격 입력" 자체를 먼저 만들기로 함.
  `client/src/features/announcements/priceOverrides.ts`의 `usePriceOverrides(announcementId)`가
  사용자가 입력한 보증금·월세를 `localStorage`에 공고 id 단위로 저장한다 — **서버에 안 보낸다.**
  v1은 회원 인증이 없어서(11절 "UserProfile은 클라이언트 보관") 이 값도 같은 원칙을 따른다.
  `AnnouncementDetailPage.tsx`의 `MoneyInput` 컴포넌트가 두 표(주택형별 공급 정보 / 모집세대) 모두에
  들어가서, API 값이 있으면 미리 채우고(사용자가 그래도 수정 가능 — D9 "가정값은 조정 가능해야 한다"와
  같은 이유) 없으면 빈 칸으로 두고 사용자가 입력하게 한다. 실제로 값이 API와 다르면 "직접입력" 배지가,
  API 자체에 값이 없으면 "원문 참조" 안내가 뜬다. 입력은 사람이 쓰기 편한 만원 단위로 받고 내부적으로
  원 단위로 환산해서 저장 — 5절의 "어떤 필드도 만원 단위로 저장하지 않는다"를 로컬 상태에도 지킴.
  타입체크·Vite HMR 컴파일 정상 확인. **로컬스토리지 기반이라 다른 기기·브라우저에서는 안 보인다** — v1
  범위에서는 의도된 제약(계정 시스템 없음).

- **가격 입력 UI 되돌림 + 별도 "자금 계산기" 메뉴로 전환 (2026-09-02)** — 공고 상세 화면 표 안에 보증금/월세
  입력칸을 넣었더니 사용자가 반려: "공고별 가격을 자동으로 뽑지 못하면 굳이 입력을 할 수 있게 할 필요가 없어,
  기존 UI는 그대로 가야할 것 같아". 공고 리스트/상세 화면(승인된 와이어프레임)은 순수 데이터 표시로 되돌리고
  (`MoneyInput`/`usePriceOverrides` 제거, `priceOverrides.ts` 삭제), 계산은 완전히 별도 화면
  (`/calculator`, "자금 계산기" 메뉴)에서 사용자가 모든 값을 직접 입력해 결과를 받는 방식으로 전환.
  공고 데이터와의 연결(자동 채움)은 이후 과제로 미룸 — 지금은 순수 독립 계산기.
- **자금 시뮬레이터 1호: 임대 계산기 (2026-09-02, 로드맵 "이후" 항목 착수)** — D7(분양·임대 계산 구조 분리)에
  따라 임대 전용으로 구현. 계산 로직은 서버(`server/src/modules/calculator/`: `calculator.types.ts` ·
  `lease-calculator.service.ts` · `calculator.controller.ts`, `POST /api/calculator/lease`)에 둠 —
  D3("Service는 req/res를 모른다, 나중에 AI Agent가 같은 함수를 도구로 호출해야 한다")를 따라 순수 함수로
  작성했고, 브라우저에서 계산해도 되는 단순 산수지만 처음부터 서버에 둬야 나중에 AI Agent 도구 연결 시
  다시 옮길 필요가 없음.
  **계산 방식**: 전세자금대출은 흔히 거치식(매달 이자만 내고 원금은 계약 종료 시 보증금 반환으로 정산)으로
  운용된다고 가정 — 그래서 "월 상환액"을 원리금균등이 아니라 `대출필요액 × 연금리 / 12`(이자만)로 계산한다.
  대출필요액 = `max(0, 보증금 - 자기자본)`. D9를 그대로 지킴 — 금리는 사용자가 입력하는 가정값이고, 결과는
  "가정값 기준 참고치"로만 표시하며 대출 승인을 단정하는 문구를 쓰지 않는다.
  클라이언트: `client/src/features/calculator/`(types·api·`LeaseCalculatorPage.tsx`), 상단 메뉴는
  `client/src/layout/AppLayout.tsx`(공고 리스트/자금 계산기 링크, `react-router-dom`의 중첩 라우트로 감쌈 —
  기존 화면 내부 마크업은 전혀 안 건드림). `formatWon`은 계산기 화면에서도 필요해져서
  `features/announcements/format.ts` → `lib/format.ts`(공용)로 이동, `apiPost` 헬퍼를 `lib/api.ts`에 추가.
  실제 서버·클라이언트 기동 후 Vite 프록시로 `POST /api/calculator/lease` 호출해 계산값(보증금 2억·자기자본
  1억·금리 3.5% → 대출 1억, 월이자 291,667원) 검증 완료, 음수 입력 시 400 검증 에러도 확인.
  **아직 공고 데이터와 연결 안 됨** — 사용자가 모든 값을 직접 입력하는 독립 계산기 단계.

- **임대 계산기 — 상환방식 3종 추가 (2026-09-02)** — 금리 계산도 상환방식에 따라 실제로 다르게 나와야 한다는
  피드백으로 확장. `calculator.types.ts`에 `RepaymentMethod = 'bullet' | 'equalPayment' | 'equalPrincipal'` 추가.
  - `bullet`(만기일시상환, 기존 로직): 매달 이자만, 원금은 만기에 보증금 반환금으로 정산.
  - `equalPayment`(원리금균등상환): 표준 원리금균등 공식(`P·r·(1+r)^n / ((1+r)^n - 1)`)으로 매달 동일한 상환액.
  - `equalPrincipal`(원금균등상환): 원금은 매달 동일(`P/n`), 이자는 남은 원금 기준이라 매달 줄어듦 —
    그래서 결과에 "첫 달"과 "마지막 달" 상환액을 따로 보여준다(다른 두 방식은 매달 동일해서 한 값만 표시).
  대출 기간(`loanTermMonths`)이 새 필수 입력값이 됨 — 만기일시상환도 총 이자 계산에 필요.
  `lease-calculator.service.ts`에 세 방식을 각각 순수 함수로 분리(`calculateBullet`/`calculateEqualPayment`/
  `calculateEqualPrincipal`)하고 `calculateSchedule`이 방식에 따라 분기 — D3(서버가 req/res 모르는 순수 함수)
  그대로 유지. 컨트롤러에서 `repaymentMethod`가 3개 값 중 하나인지 검증(아니면 400).
  클라이언트는 라디오 버튼 3개(`REPAYMENT_METHOD_OPTIONS`)와 대출기간(년) 입력을 추가, 결과 화면은
  원금균등일 때만 "마지막 달 상환액" 행을 더 보여줌.
  **실제 API 호출로 세 방식 다 검증**(대출 1억·3.5%·24개월 기준): 만기일시 총이자 7,000,008원 >
  원리금균등 총이자 3,686,528원 > 원금균등 총이자 3,645,833원 — 원금을 더 빨리 갚는 방식일수록 총이자가
  적어지는 방향으로 순서가 맞음. 상환방식 잘못된 값(`"unknown"`) 입력 시 400 검증 에러도 확인.

- **매매·분양 계산기 신설 — 매매/분양 둘 다 커버 (2026-09-16)** — "자금 계산기에 매매도 들어갈 수 있나?"라는
  질문에서 시작. D7이 이미 `SalePlan`(계약금→중도금→잔금) 자리를 마련해뒀던 걸 실제로 구현.
  **매매(기존 주택)와 분양(신규 청약)을 하나의 계산기로 커버**하되 D7의 "임대와는 절대 합치지 않는다"는
  그대로 지켜서 임대 계산기와는 완전히 별도 화면(`/calculator/sale`)으로 만듦. 매매는 중도금 회차 0으로,
  분양은 중도금 회차 N으로 표현해서 계약금→(중도금 N회, 매매면 생략)→잔금 하나의 모델로 통일.
  - 상환방식 로직(`bullet`/`equalPayment`/`equalPrincipal`)을 `repayment-schedule.ts`로 분리해서
    임대·매매 계산기 둘 다 재사용 — D7 "공통 인터페이스만 맞춘다"를 그대로 구현.
  - **핵심은 단일 숫자가 아니라 단계별 타임라인**이다 — D7 "'빌릴 수 있냐'가 아니라 '각 시점에 현금이
    마르지 않냐'"를 그대로 구현: `calculateSalePlan`이 계약금 → 중도금 각 회차 → 잔금 순으로 자기자본을
    소진시켜가며, 각 단계에서 대출로도 못 메꾸는 부족분(`shortfallWon`)이 있는지 계산한다.
  - **계약금은 전액 자기자본 가정** — D7 "계약금엔 대출이 거의 안 붙는다"를 그대로 반영, `loanCoveredWon`을
    계약금 단계에서 항상 0으로 고정.
  - 중도금은 회차별로 총액을 균등분할하고, 각 회차 중 사용자가 입력한 비율만큼 중도금대출로 충당(나머지는
    그 시점 자기자본에서 지출) — 실제 중도금 상환 스케줄이 항상 균등하지는 않지만 v1은 단순화된 가정.
    **중도금대출 이자는 계산에 안 넣었다**(무이자 집단대출 관행 가정, 화면에 명시) — 실제로는 이자가 붙는
    경우도 있어 다음 단계 후보로 남겨둠.
  - 잔금 단계에서 중도금대출 원금을 전부 정산(갚아야 할 잔금 + 중도금대출 원금)하고, 부족분을 주택담보대출로
    채우되 **LTV 한도로 캡을 건다** — 필요 대출액이 LTV 한도를 넘으면 `exceedsLtvLimit=true`로 표시하고
    그 초과분은 자기자본으로도 대출로도 못 메꾸는 진짜 부족분(`shortfallWon`)이 된다. D9대로 승인을
    단정하지 않고 "가정값 기준 참고치"로만 안내.
  - 타입: `SaleCalculationInput`/`SaleCalculationResult`/`SalePaymentStage`(`calculator.types.ts`),
    서비스: `sale-calculator.service.ts`, 라우트: `POST /api/calculator/sale`.
  - 클라이언트: `SaleCalculatorPage.tsx` — "매매(기존 주택)" / "분양(신규 청약)" 라디오로 중도금 입력칸을
    보이거나 숨김(선택은 매매가 기본), 단계별 표는 부족분이 있는 행을 빨간 배경으로 강조, 요약에 LTV 한도
    초과 여부·주담대 월상환액(원금균등이면 첫/마지막 달 따로)·총이자·최종 잔여자기자본을 보여줌.
    상단 메뉴(`AppLayout.tsx`)를 "공고 리스트 / 임대 계산기 / 매매·분양 계산기" 3개로 재구성 — 기존
    임대 계산기 URL을 `/calculator` → `/calculator/lease`로 옮김(공개 전 단계라 문제 없음).
  **실제 API 호출로 검증**: (1) 매매(중도금 0회, 5억·계약금10%·자기자본2억·LTV70%) → 부족분 없이 주담대
  3억으로 완결. (2) 분양(중도금 4회·60%, 자기자본 1억) → 잔금 단계에서 필요 대출액이 LTV 한도(3.5억)를
  초과해서 실제로 5천만원 부족분이 표시됨(`hasShortfall=true`) — LTV 캡이 진짜 부족 시나리오를 잡아내는
  것까지 확인. (3) 중도금 0회인데 중도금 비율이 0이 아닌 모순 입력 시 400 검증 에러도 확인.
  타입체크·Vite 프록시 왕복 정상 확인.

- **수동 공고 등록 — 네 번째 수집 방식 "MANUAL" (2026-09-16)** — SH 등 API가 없는 기관 공고를 PDF로 받았을 때,
  이 세션의 AI(Claude)가 PDF를 직접 읽고 값을 채워서 저장하는 방식으로 진행하기로 함(사용자가 직접 타이핑하는
  폼 UI는 이번엔 안 만듦 — 2026-09 대화에서 두 옵션 중 "AI가 읽고 올리는 방식"을 선택).
  D4(소스 어댑터 패턴)의 네 번째 수집 방식으로 자리잡음 — LH가 "오픈API"라는 수집 방식이듯 MANUAL은
  "사람(AI)이 읽고 입력"이라는 수집 방식일 뿐, 공통 `Announcement`/`HouseType`/`PurchaseLeaseSupply` 모양은 동일하게 맞춘다.
  `AnnouncementSource`를 `'LH' | 'MANUAL'`로 확장(서버 `announcement.types.ts` + 세 모델의 `SOURCES` 배열 +
  클라이언트 타입). `server/src/ingest/manual/`에 `manual.types.ts`(`ManualAnnouncementInput`)와
  `manual.service.ts`(`saveManualAnnouncement` — D2대로 외부 API 호출 없는 순수 함수)를 신설.
  재조회할 API가 없으므로 `sourceKey.panId`는 제목+게시일 기준 합성 키(`MANUAL-YYYYMMDD-제목슬러그`)로
  만들어서 같은 공고를 다시 저장해도 upsert로 갱신되게 함(중복 안 생김 — 실제로 두 번 호출해서 같은 id로
  되돌아오는 것 확인).
  원문 URL이 없을 수 있어서(PDF만 있고 웹 링크가 없는 경우) `originalUrl`을 빈 문자열 허용 — 클라이언트
  상세 화면의 "원문 공고 보기" 버튼을 `data.originalUrl`이 있을 때만 렌더링하도록 수정.
  **"사용자에게 보이는 모든 텍스트는 한글" 규정 때문에** 화면에 `source` 값을 그대로 찍던 곳들
  (`출처 {item.source}` 등)을 `SOURCE_LABELS`(`{LH:'LH', MANUAL:'직접등록'}`) 매핑을 거치도록 수정.
  **실제 저장→조회 파이프라인을 테스트 공고로 검증**: 저장(id 발급) → 같은 입력으로 재호출(같은 id로 upsert
  확인) → `GET /api/announcements/:id`로 `source:"MANUAL"`, houseTypes 포함 정상 조회 → `GET
  /api/announcements?q=...`로 검색까지 정상 확인. **검증 중 발견**: curl로 한글 쿼리스트링을 보낼 때
  이 Windows Git Bash 환경의 로케일 때문에 `--data-urlencode`가 깨진 인코딩을 보내는 현상이 있었음(서버·코드
  버그 아님, Node `fetch`+`encodeURIComponent`로는 정상) — 앞으로 이 환경에서 한글 쿼리 curl 테스트가 이상하게
  0건 나오면 먼저 인코딩부터 의심할 것. 테스트 데이터는 검증 후 정리해서 DB에 안 남김.
  **아직 실제 PDF로는 안 해봄** — 사용자가 PDF를 주면 그때 실제로 읽고 이 파이프라인으로 등록.

- **온디맨드 수집 API — 원하는 시간에 직접 트리거 (2026-09-16)** — 배포 비용 얘기 중, 매일 9시 자동 수집을
  유지하려면 서버가 24시간 떠 있어야 해서 무료 호스팅(요청 없으면 재우는 방식)과 안 맞는다는 문제가 나왔고,
  대안으로 "내가 원하는 시간에 직접 수집을 요청"하는 방식을 추가함.
  D2가 애초에 "수집 진입점은 순수 함수 하나, 호출자가 온디맨드든 스케줄러든 무관하다"고 설계해둔 덕분에
  `ingestLhAnnouncements()`에 HTTP 호출자를 하나 더 붙이기만 하면 됐다 — 새 코드는 거의 다 얇은 컨트롤러.
  `POST /api/ingest/lh`(`server/src/ingest/lh/ingest.controller.ts`) 신설, 바디로 `{from, to}`(YYYYMMDD)
  선택적으로 받고 없으면 기존 기본 범위(오늘 기준 최근 2개월)를 그대로 씀.
  **v1은 회원 인증이 없어서(11절)**, 배포됐을 때 아무나 이 엔드포인트를 두드려 공공데이터포털 일일
  호출량(10,000회)을 소진시키지 못하게 `ADMIN_INGEST_KEY` 공유키를 헤더(`x-admin-key`)로 검사하는 최소한의
  방어를 넣었다. 키가 서버에 설정 안 돼 있으면 엔드포인트 자체가 503으로 막힌다(`INGEST_DISABLED`).
  **이걸로 배포 방식 선택지가 하나 늘었다**: 스케줄러가 정확한 시각에 돌게 하려고 서버를 24시간 켜둘 필요 없이,
  프론트에 "지금 새로고침" 버튼을 만들거나 사용자가 필요할 때 이 API를 직접 호출하는 방식으로도 갈 수 있다
  (서버가 요청 들어올 때만 깨어나는 무료 호스팅과도 잘 맞음) — 자동 스케줄과 온디맨드를 병행할지, 온디맨드로
  대체할지는 아직 결정 안 함.
  실제 호출로 검증: 키 없음/틀린 키 401, 날짜 형식 틀리면 400, 올바른 키+좁은 범위(최근 3일)로 실제 수집
  성공(4건 저장) 확인.

- **배포 구조 결정 — 서버 하나에 프론트+백엔드 통합 (2026-09-16)** — 지인 몇 명에게 공유하고 싶다는 요청으로
  배포 착수. client/server 두 개를 따로 배포(Vercel+Render 등, 도메인 2개, CORS 필요)할지 하나로 합칠지
  물어봤고 "통합" 선택 — 도메인 하나, CORS 불필요, 관리 포인트 하나.
  `server/src/app.ts`가 `isProduction`일 때 `express.static(CLIENT_DIST_DIR)` + SPA 폴백 라우트를 추가로
  마운트해서 client 빌드 결과물(`client/dist`)까지 같이 서빙한다. `CLIENT_DIST_DIR`은 컴파일된
  `server/dist/app.js` 기준 `../../client/dist`(저장소 루트 기준 `client/dist`) — client/server가 여전히
  독립 npm 프로젝트라 이렇게 상대 경로로만 연결된다(진짜 모노레포로 합친 게 아님).
  API 404는 그대로 JSON 봉투(`/api` 전용 `notFoundHandler`), 그 외 모든 GET은 정적 파일 시도 후 없으면
  `index.html`로 떨어뜨려 React Router가 처리하게 한다. 개발 모드는 기존 Vite 프록시 방식 그대로 안 건드림
  (이 분기는 `isProduction`일 때만 탐).
  **실제 빌드+프로덕션 모드 실행으로 검증** — `client && npm run build` → `server && npm run build` →
  `NODE_ENV=production node dist/index.js`로 실제 기동해서 루트(`/`)·클라이언트 라우트(`/calculator/lease`,
  `/announcements/:id`)·정적 자산·실제 API·존재하지 않는 API 경로(JSON 404) 전부 확인.
  **실제로 걸린 문제 — Express 5의 와일드카드 라우트 문법 변경**: `app.get('*', ...)`을 그대로 썼더니
  부팅 자체가 `PathError: Missing parameter name at index 1: *`로 실패했다. Express 5(path-to-regexp v8)는
  이름 없는 `*`를 더 이상 안 받고 **이름 붙은 와일드카드**(`app.get('/*splat', ...)`)가 필요하다 — 문서를
  안 찾아보고 Express 4 방식 그대로 썼다가 실제 실행에서 걸러진 사례. 다른 Express 5 코드에서도 `*` 라우트를
  쓰게 되면 이 문법을 기억할 것.
  **아직 안 한 것**: 실제 호스팅(Render 등)에 올리기, MongoDB Atlas 네트워크 액세스를 배포 서버 IP에 열기,
  운영 환경변수(`MONGO_URI` 등) 호스팅 플랫폼에 등록, `ADMIN_INGEST_KEY` 재발급(로컬 개발용으로 만든 값이
  이미 대화에 노출됐으므로 실제 배포 전 새로 발급 권장).

- **Render 배포 진행 (2026-09-16~17)** — Free 플랜으로 웹서비스 생성. 실제로 겪은 문제 두 가지, 둘 다 실제
  배포 로그로 확인:
  1. Build Command에서 `NODE_ENV=production`이 설정된 채로 `npm install`이 돌면 **npm이 devDependencies를
     건너뛴다** — `vite`/`typescript`가 devDependencies라 클라이언트 `tsc -b`가 `Cannot find type
     definition file for 'vite/client'`로 실패했다. 해결: Build Command에 `--include=dev` 추가
     (`npm install --include=dev`) — 빌드 때만 강제로 devDependencies도 설치, 런타임(`npm start`)에는
     영향 없음.
  2. "Add from .env"로 환경변수를 한 번에 가져오면 로컬 `.env`의 `NODE_ENV=development`가 그대로 들어온다 —
     Render 쪽에서만 `production`으로 수동 정정 필요(로컬과 배포가 이 값 하나만 달라야 하는 상황이라
     일괄 가져오기 이후 예외적으로 손봐야 하는 필드).
  MongoDB Atlas Network Access는 이미 `0.0.0.0/0`(Allow from Anywhere)로 예전부터 열려 있어서 추가 조치 불필요했음.
  배포 성공 후 사용자가 "일단 꺼두고 싶다" — **삭제가 아니라 Settings → Delete or suspend → Suspend Web
  Service로 일시정지**(설정·환경변수는 그대로 보존, 나중에 재개만 하면 됨).
  Render 무료 플랜: 월 750시간(사실상 24시간 상시 가능)까지 무료지만 **15분 무요청 시 자동 슬립** — 이게
  매일 9시 자동 스케줄과 안 맞아서 온디맨드 수집 API를 만든 배경이 됨(위 항목 참고). 관리상 별도로 끌 필요는
  없고, 켜두면 트래픽 있을 때만 깨어난다.
- **온디맨드 수집으로 데이터 갱신 (2026-09-17)** — `POST /api/ingest/lh`를 기본 범위(오늘 기준 최근 2개월)로
  두 차례 실제 호출 — 09-16에 108건, 09-17에 110건으로 갱신 확인. 매번 스크립트 안 짜고 이 엔드포인트로
  바로 갱신 가능한 것까지 실사용으로 검증.
- **공고 리스트 정렬을 "최신순" 고정 (2026-09-17)** — 기존엔 `closingAt: 1`(마감 임박순)이 기본 정렬이었는데
  사용자가 "항상 최신순으로" 요청. `announcement.service.ts`의 `searchAnnouncements`를
  `.sort({ postedAt: -1, _id: -1 })`로 변경 — 게시일 내림차순, 같은 게시일이면 `_id` 내림차순으로 페이지네이션이
  안 흔들리게 타이브레이커를 둠. 마감임박순 정렬 옵션 자체를 없앤 것 — 필요해지면 정렬 기준을 파라미터로
  받는 방식으로 확장할 것(지금은 요청받은 대로 고정값). 실제 API 호출로 게시일 내림차순 확인.

- **엑셀 뷰어 — 독립 도구, 정렬 가능 (2026-09-17)** — 사용자가 LH 매입임대 공고에 흔히 첨부되는
  "공급가능주택 목록" 엑셀(순번·지자체명·도로명주소·동·호·주택형·전용/공용면적·방수·층수·승강기유무·
  주택유형·보증금·임대료·무보증월세여부, 단지별 아님 **개별 호실 단위**) 캡처를 보여주며 업로드+정렬
  화면이 가능한지 물어서 신설. **이 엑셀엔 API로는 못 얻는 실제 보증금·임대료 숫자가 있다** — 6절에 이미
  기록된 "매입임대·전세임대(130~145)는 API에 가격 필드가 없다"는 공백을 이 경로로 메꿀 수 있다는 뜻.
  두 가지를 물어서 정함: (1) 화면에 직접 업로드 버튼 vs 채팅에 공유 → **업로드 버튼**, (2) 특정 공고에
  귀속 vs 독립 뷰어 → **독립 뷰어**(어떤 엑셀이든 그 자리에서 표로 보여주는 범용 도구, 저장 안 함).
  독립+비영속이라 **서버를 안 거치고 클라이언트에서만** 파싱하기로 함 — 새 API 엔드포인트도, Mongoose
  모델도, 파일 업로드 미들웨어도 필요 없음. `client/src/features/excel-viewer/ExcelViewerPage.tsx`
  (`/excel-viewer`, 상단 메뉴에 "엑셀 뷰어" 추가) 하나로 끝남.
  **`xlsx`(SheetJS) 설치 시 주의할 점 발견** — `npm install xlsx`로 받아지는 npm 레지스트리판은
  **패치가 없다**(`npm audit`에서 Prototype Pollution·ReDoS 취약점 2건이 "No fix available"로 뜸 — SheetJS가
  라이선스 이슈로 npm 레지스트리 갱신을 중단하고 자체 CDN(`cdn.sheetjs.com`)에만 패치판을 올리기 때문,
  웹검색으로 확인). 그래서 `npm uninstall xlsx` 후 SheetJS 공식 문서가 안내하는 방식대로
  `npm install --save https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`로 재설치 — `npm audit` 0건 확인.
  이 회사 네트워크의 TLS 인터셉션 때문에(이 세션에서 몇 번 겪은 것과 같은 증상) 이 설치 한 번은
  `--strict-ssl=false`로 우회함 — 코드에 반영되는 설정이 아니라 이번 설치 명령 한 번에만 적용됨.
  **헤더 행 자동 감지**: 실제 공고 엑셀은 제목·공고명·안내문 행이 먼저 나오고 진짜 헤더는 4~5번째 행에
  있는 경우가 많다(사용자 캡처가 정확히 이 구조) — 상위 15개 행 중 "채워진 셀이 가장 많은 행"을 헤더로
  추정하고, 사용자가 숫자 입력으로 직접 고칠 수 있게 함(자동 추정이 항상 맞는다고 단정 안 함).
  **숫자 컬럼 자동 판별**: 콤마·₩·% 제거 후 숫자로 파싱되는 값이 80% 이상이면 그 컬럼은 숫자 정렬,
  아니면 문자열 정렬 — "6,754,000" 같은 보증금 문자열도 숫자로 정확히 정렬된다.
  **캡처와 최대한 비슷한 실제 엑셀 파일(SheetJS로 직접 생성)을 만들어 전체 파이프라인(쓰기→읽기→헤더
  감지→정렬)을 실행까지 확인** — 헤더 행 인덱스 정확히 감지, 데이터 5행 정확히 추출, 보증금 오름차순·
  전용면적 내림차순 둘 다 기대한 순서로 정렬됨을 확인. 타입체크·Vite 컴파일도 정상.

  **실사용 피드백으로 발견한 한계 — 2단 병합 헤더 (2026-09-17):** LH 매입임대 엑셀은 "면적(㎡)" 한 칸 밑에
  "전용"/"공용"이, "임대조건(원)" 한 칸 밑에 "보증금"/"임대료"가 따로 있는 **2단 헤더** 구조다. 단일 행만
  헤더로 잡는 최초 버전은 이 경우 "전용"/"공용" 대신 "면적(㎡)"/"열8"처럼 엉뚱한 라벨을 뽑았다(사용자가
  실제로 "지자체명, 도로명주소, ... 전용, 공용, ..."이 안 나온다고 지적해서 발견). **헤더를 단일 행이 아니라
  "행 범위"로 받도록 수정** — `headerRowStart`~`headerRowEnd` 사이에서 컬럼마다 "범위 안에서 가장 아래쪽의
  비어있지 않은 셀"을 라벨로 채택(`mergeHeaderRange`). 병합 셀은 상위 행에만 값이 있고 하위 행은 비어
  있는 경우가 많아서, 하위 행에 실제 값이 있으면 그걸 우선하고 없으면 상위 행 값으로 자연히 대체된다.
  자동 추정은 여전히 한 줄(기존 로직)이고, "다음 줄과 합치기" 버튼으로 한 번에 범위를 넓힐 수 있게 UI를
  바꿈. 위와 같은 2단 헤더 샘플을 직접 만들어 재검증 — 범위를 한 줄→두 줄로 넓히면 "면적(㎡)"/"열8"이
  정확히 "전용"/"공용"으로, "임대조건(원)"/"열14"가 "보증금"/"임대료"로 바뀌는 것까지 확인.

### 진행 중 — 다음 순서
실제 호스팅 계속 진행(재개 시점 결정은 사용자 몫). 그 외 PDF 수동 등록, 매매/분양 계산기 확인, 중도금대출 이자 반영, DSR/자격 판정 엔진 등도 대기 중.

### 이후
- 청약홈 어댑터 추가 (통합 모델 검증)
- 자금 시뮬레이터 (외부 API 불필요. 순수 계산 로직)
- 자격 판정 엔진
- AI Agent (앞 함수들을 도구로 호출)

---

## 11. requirements.md 와의 차이

`docs/requirements.md` 는 v0.1이며 아래 항목이 이후 논의로 변경되었다. **이 파일이 우선한다.**

- 서비스 무게중심: 공고 검색 → **자금 설계 시뮬레이터**
- 회원 인증: v1에서 **제외** (UserProfile은 클라이언트 보관)
- 첫 소스: 실거래가 → **LH 공고 API**
- 경매: 제외 권고 → **보류** (결정 유예)
- 대상 지역: 수도권 한정

---

## 12. 절대 규칙

- 확인되지 않은 외부 데이터를 상상하지 않는다. Mock 데이터는 명확히 구분한다.
- 기존 프로젝트 구조나 코드를 추측해서 존재한다고 가정하지 않는다. 확인이 안 되면 파일 내용을 요청한다.
- 실행하지 않은 코드를 실행했다고 표현하지 않는다.
- 확인하지 않은 API나 라이브러리 동작을 사실처럼 단정하지 않는다.
- 기존 기능을 제거하거나 동작 방식을 변경할 경우 반드시 사용자에게 알린다.
